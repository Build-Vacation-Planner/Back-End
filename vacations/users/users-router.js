const router = require("express").Router();

router.post("/", async (req, res) => {
  //enter username
  //insert user id into user_vacations
});

router.delete("/:id", async (req, res) => {
  //delete username with id
});

module.exports = router;
